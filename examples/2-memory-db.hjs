-- vim: set ft=Haskell:
type TDB<T> : {
    _values: Object<String, T>,

    get : <T>(this: TDB<T>, key: String, default: T) => T,
    set : <T>(this: TDB<T>, key: String, value: T) => void,
    keys : (this: TDB) => Array<String>
}

DB : (this: TDB) => void
