import { TypeChecker } from "../index.hjs"

export default {
    prettyPrintAllErrors: (checker: TypeChecker) => void,
    prettyPrintAllErrorsWithTrace : (checker: TypeChecker) => void,
    prettyPrintTraces : (checker: TypeChecker) => void
}
