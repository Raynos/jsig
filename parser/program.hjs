import { JsigProgramNode } from "../ast/index.hjs"

type Result : {
    status: Boolean,
    value?: JsigProgramNode,
    index?: {
        line: Number
    }
}

type Parser : {
    parse: (input: String) => Result
}

export default Parser
